---
- name: Saul Becarra's account
  become:
    yes
  hosts:
    kmap.arizona.edu:uamap-dev.arl.arizona.edu:dev.kmap.arizona.edu:research.kmap.arizona.edu
      
  tasks:

  # - name: create saulb account
  #   user:
  #     name:
  #       saulb
  #     groups:
  #       sudo
  #     append:
  #       yes
  #     comment:
  #       "Saul Becerra"
  #     shell:
  #       /bin/bash
  #     password: 
  #       "!"
      
  - name: set ssh authorized key
    authorized_key:
      user:
        saulb
      state:
        absent
      key:
        "{{ lookup('file', '/etc/ansible/keys/saulb.pub') }}"
      manage_dir:
        yes
